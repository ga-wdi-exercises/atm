var checkingBalance = $(".balance").eq(0);
// checkingBalance = parseInt( checkingBalance.innerHTML.replace("$", "") );

var savingsBalance = $(".balance").eq(1);

//var checkingDeposit = function(amount){
//  checkingBalance = parseInt( checkingBalance.innerHTML.replace("$", "") );
//};

var savDepButton = $("input").eq(1);
var savWitButton = $("input").eq(2);
var chkDepButton = $("input").eq(4);
var chkWitButton = $("input").eq(5);

function checkingDeposit() {
//user enters number into input field
//hits deposit button
    //eventListerner "input"
//total dollar amount updates
  //update value of div .balance
    //create variable = 0 for sum total; then do checkingBalance += input
}

function checkingWithdrawal(amount){

}

function savingsDeposit(amount){

}

function savingsWithdrawal(amount){

}

// an eventListerner for each button, each one a "click"
