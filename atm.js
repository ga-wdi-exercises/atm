/* global $ */

$(document).ready(function () {
// Uh oh -- it's saying `$` is undefined! Something's missing from `index.html`...
})

// make background red as a test
$('html').css('background', 'red')

// create a class = atm
class ATM {
  constructor (name, begBal = 0, backupAcc = null) {

  }
}
  // constructor includes (name, starting balance = 0, backupAccount = null)
    // name
    // starting balance
    // backup
  // methods
    // deposit
    // withdraw (calls verify balance)

    // verify balance () = checks the money in the account
